import { Texture } from "../../libs/three";

export default class GameOverPage {
  constructor (callbacks) {
    this.callbacks = callbacks
  }
  init (options) {
    console.log('game over page init')
    this.initGameoverCanvas(options)
  }
  initGameoverCanvas (options) { 
  }
  show () {
    
  }
  hide () {
    
  }
}